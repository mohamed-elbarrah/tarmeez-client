/**
 * Re-export the shared Header from storefront/core.
 * All themes share the same Header component — customization
 * happens via CSS custom properties (--p-color, --s-color, etc.).
 */
export { default } from "@/components/storefront/core/Header";
