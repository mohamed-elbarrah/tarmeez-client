/**
 * Re-export the shared Footer from storefront/core.
 * All themes share the same Footer component — customization
 * happens via CSS custom properties (--p-color, etc.).
 */
export { default } from "@/components/storefront/core/Footer";
